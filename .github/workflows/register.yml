  # push:
  # branches:
  # - main
#jobs:
#  build-and-push:
#    runs-on: ubuntu-latest
#    steps:
#      - name: Checkout
#        uses: actions/checkout@v2
#      - name: GCP Auth
#        id: auth
#        uses: google-github-actions/auth@v2
#        with:
#          credentials_json: '${{secrets.GCP_CREDS}}'
#
#      - name: Set up gcloud CLI
#        uses: google-github-actions/setup-gcloud@v2
#
#      - name: Configure Docker to use Artifact Registry
#        run: gcloud auth configure-docker us-central1-docker.pkg.dev
#
#      - name: Push Docker image to Artifact Registry
#        run: docker push us-central1-docker.pkg.dev/laboratorio1-447417/mlflow-mlops-g57/wine-class-api:latest

name: Deploy Image GCP/GitHubActions
on:
  [workflow_dispatch]
jobs:
  auth-gcp:
    runs-on: ubuntu-latest
    steps:
      - name: GCP Auth
        id: auth
        uses: google-github-actions/auth@v2
        with:
          credentials_json: '${{secrets.GCP_CREDS}}'

      - name: Set up gcloud CLI
        uses: google-github-actions/setup-gcloud@v2

      - name: Configure Docker to use Artifact Registry
        run: gcloud auth configure-docker us-central1-docker.pkg.dev

      - name: Deploy to Cloud Run
        uses: google-github-actions/deploy-cloudrun@v2
        with:
          service: ${{ vars.GCP_IMG_CANNONICAL }}-adp2
          region: us-central1
          image: us-central1-docker.pkg.dev/${{ vars.GCP_PROJECT_LAB }}/${{ vars.GCP_IMG_REPO }}/${{ vars.GCP_IMG_CANNONICAL }}:${{ vars.GCP_IMG_VER }}
          env_vars: |
            PORT=8000
          allow_unauthenticated: true